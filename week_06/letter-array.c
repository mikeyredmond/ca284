#include <stdio.h>
#include <stdlib.h>

int main (void)
{

int letters[10];




printf ("The total size of the letters array is: %zu\n\n",sizeof(letters)); /* It goes and checks how much memory has been allocated to letters */

return(0);
}